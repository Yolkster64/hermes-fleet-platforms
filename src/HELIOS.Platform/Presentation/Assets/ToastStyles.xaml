<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:HELIOS.Platform.Presentation.Components">

    <!-- ==================== TOAST NOTIFICATION STYLES ==================== -->
    
    <!-- Base Toast Container Style -->
    <Style x:Key="ToastContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,3,0,0"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="MaxWidth" Value="500"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="4" Opacity="0.15"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Success Toast Style -->
    <Style x:Key="SuccessToastStyle" TargetType="Border" BasedOn="{StaticResource ToastContainerStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
    </Style>

    <!-- Warning Toast Style -->
    <Style x:Key="WarningToastStyle" TargetType="Border" BasedOn="{StaticResource ToastContainerStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
    </Style>

    <!-- Error Toast Style -->
    <Style x:Key="ErrorToastStyle" TargetType="Border" BasedOn="{StaticResource ToastContainerStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
    </Style>

    <!-- Info Toast Style -->
    <Style x:Key="InfoToastStyle" TargetType="Border" BasedOn="{StaticResource ToastContainerStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource InfoBrush}"/>
    </Style>

    <!-- Toast Title Text Style -->
    <Style x:Key="ToastTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
    </Style>

    <!-- Toast Message Text Style -->
    <Style x:Key="ToastMessageStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="LineHeight" Value="16"/>
    </Style>

    <!-- ==================== TOAST CLOSE BUTTON ==================== -->
    
    <Style x:Key="ToastCloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Content" Value="✕"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== PROGRESS TOAST ==================== -->
    
    <Style x:Key="ProgressToastStyle" TargetType="Border" BasedOn="{StaticResource InfoToastStyle}">
        <Setter Property="Padding" Value="16"/>
    </Style>

</ResourceDictionary>
