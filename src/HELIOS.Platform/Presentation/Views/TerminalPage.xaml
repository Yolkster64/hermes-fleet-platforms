<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HELIOS.Platform.Presentation.Views.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HELIOS.Platform.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20">
        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,15">
            <TextBlock Text="HELIOS CLI Terminal" Style="{StaticResource HeaderTextBlockStyle}" FontSize="32"/>
            <TextBlock Text="Execute commands and manage system operations" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Terminal Output -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Padding="0" Margin="0,0,0,15">
            <Grid RowDefinitions="*,Auto">
                <!-- Output Area -->
                <ScrollViewer Grid.Row="0">
                    <TextBlock x:Name="OutputText" 
                               Text="HELIOS CLI v1.0.0 - Type 'help' for available commands&#x0A;&#x0A;&gt; "
                               FontFamily="Consolas" FontSize="12"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                               Padding="15"
                               TextWrapping="Wrap"
                               SelectionForeground="{ThemeResource TextSelectionHighlightColor}"
                               SelectionHighlightColor="{ThemeResource TextSelectionHighlightColor}"/>
                </ScrollViewer>

                <!-- Input Section -->
                <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="10" Padding="15,10,15,15" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefault}">
                    <TextBox x:Name="CommandInput" 
                             PlaceholderText="Enter command here..."
                             FontFamily="Consolas" FontSize="12"
                             AcceptsReturn="False"
                             KeyDown="CommandInput_KeyDown"/>
                    <Button Grid.Column="1" Content="Execute" Click="ExecuteButton_Click" Width="80"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Quick Commands -->
        <StackPanel Grid.Row="2" Spacing="10">
            <TextBlock Text="Quick Commands" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="13"/>
            <ScrollView HorizontalScrollMode="Auto">
                <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,0,0,10">
                    <Button Content="status" Click="QuickCommand_Click" Tag="status"/>
                    <Button Content="services list" Click="QuickCommand_Click" Tag="services list"/>
                    <Button Content="performance" Click="QuickCommand_Click" Tag="performance"/>
                    <Button Content="disk info" Click="QuickCommand_Click" Tag="disk info"/>
                    <Button Content="network status" Click="QuickCommand_Click" Tag="network status"/>
                    <Button Content="processes" Click="QuickCommand_Click" Tag="processes"/>
                    <Button Content="clear" Click="QuickCommand_Click" Tag="clear"/>
                    <Button Content="help" Click="QuickCommand_Click" Tag="help"/>
                </StackPanel>
            </ScrollView>
        </StackPanel>
    </Grid>
</Page>
