<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HELIOS.Platform.Presentation.Views.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HELIOS.Platform.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*" Padding="20">
        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="System Tools" Style="{StaticResource HeaderTextBlockStyle}" FontSize="32"/>
            <TextBlock Text="Access diagnostic and maintenance utilities" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Tools Grid -->
        <ScrollView Grid.Row="1">
            <StackPanel Spacing="20" Padding="0,0,20,20">

                <!-- System Diagnostics Section -->
                <StackPanel Spacing="10">
                    <TextBlock Text="System Diagnostics" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <GridView ItemsSource="{x:Bind DiagnosticTools}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Tool_Click">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="local:ToolItem">
                                <StackPanel Spacing="10" Padding="15" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                           BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                           CornerRadius="8" MinWidth="150" MinHeight="150" 
                                           PointerEntered="ToolCard_PointerEntered" PointerExited="ToolCard_PointerExited">
                                    <TextBlock Text="{x:Bind Icon}" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </StackPanel>

                <!-- Maintenance Tools Section -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Maintenance Tools" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <GridView ItemsSource="{x:Bind MaintenanceTools}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Tool_Click">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="local:ToolItem">
                                <StackPanel Spacing="10" Padding="15" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                           BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                           CornerRadius="8" MinWidth="150" MinHeight="150"
                                           PointerEntered="ToolCard_PointerEntered" PointerExited="ToolCard_PointerExited">
                                    <TextBlock Text="{x:Bind Icon}" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </StackPanel>

                <!-- Optimization Tools Section -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Optimization Tools" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <GridView ItemsSource="{x:Bind OptimizationTools}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="Tool_Click">
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="local:ToolItem">
                                <StackPanel Spacing="10" Padding="15" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                           BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                           CornerRadius="8" MinWidth="150" MinHeight="150"
                                           PointerEntered="ToolCard_PointerEntered" PointerExited="ToolCard_PointerExited">
                                    <TextBlock Text="{x:Bind Icon}" FontSize="32" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                    <TextBlock Text="{x:Bind Description}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </StackPanel>

            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
