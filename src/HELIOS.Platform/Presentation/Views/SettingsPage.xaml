<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HELIOS.Platform.Presentation.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HELIOS.Platform.Presentation.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,*" Padding="20">
        <!-- Header -->
        <StackPanel Grid.Row="0" Spacing="10" Margin="0,0,0,20">
            <TextBlock Text="System Settings" Style="{StaticResource HeaderTextBlockStyle}" FontSize="32"/>
            <TextBlock Text="Configure HELIOS Platform settings" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Settings Tabs -->
        <ScrollView Grid.Row="1">
            <StackPanel Spacing="20" Padding="0,0,20,0">
                
                <!-- General Settings -->
                <StackPanel Spacing="10">
                    <TextBlock Text="General" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="15" BorderThickness="1" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Launch at startup" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="StartupToggle" IsOn="True"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Dark theme" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="DarkThemeToggle" IsOn="True"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable notifications" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="NotificationsToggle" IsOn="True"/>
                            </Grid>
                            <Separator/>
                            <ComboBox Header="Language" PlaceholderText="Select language" Width="200" HorizontalAlignment="Left">
                                <ComboBoxItem Content="English"/>
                                <ComboBoxItem Content="Español"/>
                                <ComboBoxItem Content="Français"/>
                                <ComboBoxItem Content="Deutsch"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Performance Settings -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Performance" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="15" BorderThickness="1" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <TextBlock Text="Refresh interval (seconds)"/>
                            <Slider x:Name="RefreshSlider" Minimum="1" Maximum="30" Value="5" IsThumbToolTipEnabled="True"/>
                            
                            <TextBlock Text="Background processes priority" Margin="0,10,0,0"/>
                            <ComboBox x:Name="PriorityCombo" Width="200" HorizontalAlignment="Left">
                                <ComboBoxItem Content="Low"/>
                                <ComboBoxItem Content="Normal" IsSelected="True"/>
                                <ComboBoxItem Content="High"/>
                            </ComboBox>

                            <Separator/>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable memory optimization" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="MemoryOptToggle" IsOn="True"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable GPU acceleration" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="GpuAccelToggle" IsOn="True"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Security Settings -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Security" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="15" BorderThickness="1" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Require password on startup" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="PasswordToggle" IsOn="False"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable audit logging" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="AuditLogToggle" IsOn="True"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable encryption for sensitive data" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="EncryptToggle" IsOn="True"/>
                            </Grid>
                            <Separator/>
                            <Button Content="Change password" HorizontalAlignment="Left"/>
                            <Button Content="View audit logs" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Cloud Integration -->
                <StackPanel Spacing="10">
                    <TextBlock Text="Cloud Integration" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="15" BorderThickness="1" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable cloud sync" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="CloudSyncToggle" IsOn="False"/>
                            </Grid>
                            <ComboBox Header="Cloud provider" PlaceholderText="Select provider" Width="200" HorizontalAlignment="Left">
                                <ComboBoxItem Content="Microsoft Azure"/>
                                <ComboBoxItem Content="AWS"/>
                                <ComboBoxItem Content="Google Cloud"/>
                            </ComboBox>
                            <Button Content="Configure cloud account" HorizontalAlignment="Left" IsEnabled="False"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Advanced Settings -->
                <StackPanel Spacing="10" Margin="0,0,0,30">
                    <TextBlock Text="Advanced" Style="{StaticResource SubtitleTextBlockStyle}"/>
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                            CornerRadius="8" Padding="15" BorderThickness="1" 
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <StackPanel Spacing="15">
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable developer mode" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="DevModeToggle" IsOn="False"/>
                            </Grid>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBlock Text="Enable diagnostic telemetry" VerticalAlignment="Center"/>
                                <ToggleSwitch Grid.Column="1" x:Name="DiagnosticsToggle" IsOn="True"/>
                            </Grid>
                            <Separator/>
                            <Button Content="Reset to defaults" HorizontalAlignment="Left" Background="{ThemeResource SystemErrorBackgroundColor}"/>
                            <Button Content="Export settings" HorizontalAlignment="Left"/>
                            <Button Content="Import settings" HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
